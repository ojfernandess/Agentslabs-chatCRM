# Desenvolvimento local: expõe Postgres/Redis no host para psql, DBeaver, etc.
# NÃO usar em produção / Easypanel.
#
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d
services:
  db:
    ports:
      - "127.0.0.1:5432:5432"
  redis:
    ports:
      - "127.0.0.1:6379:6379"
